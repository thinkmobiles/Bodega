<?xml version="1.0" encoding="utf-8"?>
<android.support.percent.PercentRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                               android:layout_width="match_parent"
                                               android:layout_height="match_parent"
                                               xmlns:app="http://schemas.android.com/apk/res-auto"
                                               xmlns:tools="http://schemas.android.com/tools">
    <ScrollView
        android:visibility="gone"
        android:id="@+id/svTextContainer_FILP"
        android:fadeScrollbars="false"
        app:layout_widthPercent="51%"
        android:layout_marginRight="@dimen/description_horizontal_padding"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:overScrollMode="never">
        <TextView
            android:id="@+id/tvText_FILP"
            android:textColor="@color/black"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingRight="@dimen/double_padding"
            android:paddingEnd="@dimen/double_padding"/>

    </ScrollView>

    <ImageButton
        android:id="@+id/ivImage_FILP"
        android:background="@null"
        tools:src="@drawable/background"
        android:scaleType="fitCenter"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</android.support.percent.PercentRelativeLayout>